<% provide(:title, 'ゴル友を探す') %>

<div class="page">
  <div class="user-index-panel">
    <div class="page-header d-flex align-items-center mx-auto mb-4">
      <%= paginate(@users) %>
    </div>

    <div class="users">
      <%= render partial: 'users/user_card', collection: @users, as: :user_card %>
    </div>

    <div class="all-users-footer">
      <%= paginate(@users) %>
    </div>
  </div>
</div>