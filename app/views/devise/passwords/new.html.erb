<div class="page">
  <div class="mid-white-panel">
    <h1 class="text-info">パスワードリセット</h1>

    <%= form_with(model: resource, as: resource_name, url: password_path(resource_name),
      html: { method: :post }, local: true) do |f| %>
      <%= render "devise/shared/new_form_item", form: f %>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>