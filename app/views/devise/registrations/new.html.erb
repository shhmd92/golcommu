<div class="loginpage">
  <div class="loginpanel">
    <h1 class="text-info">会員登録</h1>

    <%= form_with(model: resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/form_item", form: f %>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>