<% if error_messages.errors.any? %>
  <div class="error_explanation text-center font-weight-bold mb-4" style="max-width: 1000px">
    <span class="fas fa-exclamation-circle mr-1"></span>
    <% error_messages.errors.full_messages.each do |message| %>
      <%= message %>。
    <% end %>
  </div>
<% end %>